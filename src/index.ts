// import { LexPrime } from "@weapons/secondary/LexPrime";

// function transformTextLines(text: string[]): string {
//     return text.join("<br>");
// }

// function updateTextOnScreen() {
//     const element = document.getElementById("container");

//     let weapon = new LexPrime();

//     let text = transformTextLines([
//         `Simulated DPS for ${weapon.name}: ${weapon
//             .simulateDPS(false)
//             .toFixed(2)} DPS`,
//     ]);

//     if (element) {
//         element.innerHTML = text;
//     }
// }

// updateTextOnScreen();
